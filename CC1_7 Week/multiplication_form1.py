def tabuada():
    tab = 0
    while tab < 10:
        tab = tab + 1
        i = 0
        while i < 10:
            i += 1
            print(tab, "x", i, "=", tab*i)
        print()

# ALGORITMO

# define variaval 'tab', que e tabuada que vamos calcular, inicia a '0'
# enquanto 'tab' for menor que 10
    # adiciono 1 a 'tab' (assim iniciamos pela tabuada do 1)
    # defino a variavel 'i' que e iteracao, inicia a 'zero'
        # enquanto 'i' for menor que 10
            # somo 1 a 'i'
            # imprimo 'tab' que na primeira rodada vai sair tudo '1' x i (muda a cada rodada, de 1 ate 10)
        # dou um print para que ele pule de linha a cada mudanca de tabuada
        # como o tab esta a '1' e dentro do primeiro loop, ele nao atualiza com a primeira linha do codigo e executa o primeiro while de nov

        
            
